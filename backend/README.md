# Nuvela backend

NestJS REST API + Socket.IO gateway. See the [root README](../README.md) for setup, env vars, Swagger, and deployment.

```powershell
npm ci
npm run start:dev    # :3001
npm run test
npm run test:e2e
```

Swagger (local): http://localhost:3001/api
